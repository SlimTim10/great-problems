import qualified Data.Text as Text ( null )
import qualified Reflex.Dom.Core as R
    ( Reflex(Dynamic),
      DomBuilder,
      PostBuild,
      blank,
      dyn,
      elAttr,
      text )
import qualified Problem.Convert as Convert ( ConvertResponse )
import Global ( Text, Map, void, (=:) )
