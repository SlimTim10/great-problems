import qualified Reflex.Dom.Core as R
    ( def,
      Reflex(Dynamic, Event),
      DomBuilder(textAreaElement),
      HasSetValue(setValue),
      el,
      elAttr,
      text,
      HasValue(value) )
import Global ( Text, (&), (.~), (=:) )
