import qualified Database.PostgreSQL.Simple as SQL ( Connection )
import qualified Common.Api.User as User ( User(id, verified) )
import qualified Common.Api.Auth as Auth ( Auth )
import qualified Database.Queries as Queries
    ( authenticate,
      removeSessionsByUserId,
      newSession,
      removeSessionById,
      getUserFromSession )
import Global ( Text )
