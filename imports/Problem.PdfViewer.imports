import qualified Data.Text as Text ( null )
import qualified Reflex.Dom.Core as R
    ( Reflex(Dynamic),
      DomBuilder,
      PostBuild,
      blank,
      dyn,
      elAttr,
      elClass,
      text )
import qualified Problem.Convert as Convert
    ( ConvertResponse(errorLatex, errorIcemaker) )
import Global ( Text, Map, void, (=:) )
