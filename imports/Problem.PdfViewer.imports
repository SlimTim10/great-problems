import qualified Data.Text as Text ( null )
import qualified Reflex.Dom.Core as R
    ( blank,
      dyn_,
      el,
      elAttr,
      elClass,
      text,
      Reflex(Dynamic),
      PostBuild,
      DomBuilder )
import qualified Common.Api.Compile as Compile
    ( Response(resErrorLatex, resErrorIcemaker, resPdfContents) )
import Global ( Text, Map, (=:) )
