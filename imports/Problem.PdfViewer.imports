import qualified Data.Text as Text ( null )
import qualified Reflex.Dom.Core as R
    ( DomBuilder,
      Reflex(Dynamic),
      blank,
      dyn_,
      elAttr,
      elClass,
      text,
      PostBuild )
import qualified Common.Compile as Compile
    ( CompileResponse(errorLatex, errorIcemaker, pdfContent) )
import Global ( Text, Map, (=:) )
