import qualified Reflex.Dom.Core as R
    ( text,
      Reflex(Event),
      DomBuilder,
      HasDomEvent(domEvent),
      EventName(Click) )
import qualified MyReflex.Dom.Widget.Basic as R'
    ( elAttrClass, elAttrClass' )
import Global ( Text, (=:) )
