import qualified Data.Text as T ( empty, null )
import qualified Reflex.Dom.Core as R
    ( leftmost,
      foldDyn,
      MonadHold,
      Reflex(Event, Dynamic),
      PostBuild,
      DomBuilder )
import qualified Common.Api.Compile as Compile
    ( Response(resPdfContent, resErrorIcemaker) )
import qualified Util ( buttonDynClass )
import Global ( MonadFix, Text, isNothing )
