import qualified Data.Text as T ( null, empty )
import qualified Reflex.Dom.Core as R
    ( DomBuilder,
      MonadHold,
      Reflex(Event, Dynamic),
      leftmost,
      foldDyn,
      PostBuild )
import qualified Common.Compile as Compile
    ( CompileResponse(pdfContent, errorIcemaker) )
import qualified Util ( buttonDynClass )
import Global ( MonadFix, Text, isNothing )
