import qualified Data.Maybe as Maybe ( fromJust, isNothing )
import qualified Data.Text as T ( null )
import qualified Reflex.Dom.Core as R
    ( DomBuilder,
      MonadHold,
      Reflex(updated, Dynamic),
      leftmost,
      foldDyn,
      PostBuild )
import qualified Problem.Convert as Convert
    ( ConvertResponse(pdfContent, errorIcemaker) )
import qualified Util ( buttonDynClass )
import Global ( MonadFix, Text, bool )
